import Typography from 'typography';

const typography = new Typography({
  baseFontSize: '16px',
  baseLineHeight: 1.45,
  bodyFontFamily: ['Cardo', 'Georgia', 'serif'],
  headerFontFamily: ['Nunito', 'Helvetica', 'sans-serif']
});

export default typography;
