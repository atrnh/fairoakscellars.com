import React from 'react';
import Layout from '../components/layout';
import bg from '../images/grape-picking.jpg';

const IndexPage = () => (
  <Layout>
    <main style={{
      width: '100%',
      height: '100%'
    }}>
    </main>
  </Layout>
);

export default IndexPage
